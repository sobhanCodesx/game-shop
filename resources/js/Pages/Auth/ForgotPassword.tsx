import{Link,useForm}from"@inertiajs/react";import{ArrowRight,KeyRound,Mail}from"lucide-react";import{type FormEvent}from"react";import AuthShell,{authButton,authInput,Field}from"../../Components/Auth/AuthShell";
export default function Forgot(){const{data,setData,post,processing,errors}=useForm({email:""});return <AuthShell title="برگرد به بازی" subtitle="ایمیل حسابت را وارد کن تا کد امن تغییر رمز را دریافت کنی." eyebrow="بازیابی رمز"><form className="space-y-5" onSubmit={(e:FormEvent)=>{e.preventDefault();post("/forgot-password")}}><div className="mx-auto grid size-16 place-items-center rounded-3xl bg-violet-500/10 text-violet-400"><KeyRound size={29}/></div><Field error={errors.email} label="آدرس ایمیل حساب"><div className="relative"><Mail className="absolute right-4 top-1/2 -translate-y-1/2 text-slate-600" size={18}/><input autoFocus className={`${authInput} pr-12`} dir="ltr" onChange={e=>setData("email",e.target.value)} placeholder="name@example.com" type="email" value={data.email}/></div></Field><button className={authButton} disabled={processing}>{processing?"در حال ارسال…":"دریافت کد بازیابی"}</button><Link className="flex items-center justify-center gap-2 text-xs font-bold text-slate-500 hover:text-white" href="/login"><ArrowRight size={15}/>بازگشت به ورود</Link></form></AuthShell>}
