import{Link,useForm}from"@inertiajs/react";import{type FormEvent}from"react";import AuthShell,{authButton,authInput,Field}from"../../Components/Auth/AuthShell";
export default function Forgot(){const{data,setData,post,processing,errors}=useForm({identifier:""});return <AuthShell title="بازیابی رمز عبور" subtitle="ایمیل یا شماره موبایل حسابت را وارد کن." eyebrow="بازیابی رمز"><form className="space-y-5" onSubmit={(e:FormEvent)=>{e.preventDefault();post("/forgot-password")}}><Field error={errors.identifier} label="ایمیل یا شماره موبایل"><input autoFocus className={authInput} dir="ltr" onChange={e=>setData("identifier",e.target.value)} value={data.identifier}/></Field><button className={authButton} disabled={processing}>دریافت کد بازیابی</button><Link className="block text-center text-xs" href="/login">بازگشت به ورود</Link></form></AuthShell>}
